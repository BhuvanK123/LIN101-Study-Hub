import 'package:flutter/material.dart';

class PhoneticsQuiz extends StatelessWidget {
  const PhoneticsQuiz({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
